# Club and story only; no private mix

`#lab` was a second dual-deck mix (crate player, set engine, policy, file drops) that could never drive the shared radio. Keeping it meant two club frames and a fake seam. The product is `#club` (shared radio) and `#story`. `#lab` redirects to `#club`. ENABLE_LAB goes away. Leave mind still exists as scoring for the server show log, not as a visitor mixer.

**Status:** accepted
